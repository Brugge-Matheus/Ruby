<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <p><%= pluralize(post.view, 'view') %></p>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
